# This file generates the GitHub Actions matrix
# For simple board + shield combinations, add them
# to the top level board and shield arrays, for more
# control, add individual board + shield combinations to
# the `include` property, e.g:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#
---
include:
  # zmk is pinned to v0.3.0 in west.yml, which still uses the old board
  # name nice_nano_v2 (the nice_nano//zmk rename only exists on zmk main).
  #
  # Dongle = central (plugged into computer via USB). Holds the keymap.
  - board: nice_nano_v2
    shield: corne_dongle_pro_micro
  # Both halves are now peripherals, so the left no longer acts as
  # central relaying the right -> much less battery drain.
  - board: nice_nano_v2
    shield: corne_left_peripheral
  - board: nice_nano_v2
    shield: corne_right
