# Dongle-only overrides. Applied on top of the module's
# corne_dongle_pro_micro.conf (ZMK config dir wins on conflict).

# No display on the dongle -> don't build the display stack.
CONFIG_ZMK_DISPLAY=n
